// This file was generated by [ts-rs](https://github.com/Aleph-Alpha/ts-rs). Do not edit this file manually.
import type { CapturedCombatant } from "./CapturedCombatant";
import type { EffectSnapshot } from "./EffectSnapshot";

/**
 * Every combatant and anchored effect as they stood when `turn` began.
 *
 * # Examples
 *
 * ```
 * use shadowcat::data::engine::combat::TurnRecord;
 * use uuid::Uuid;
 *
 * let turn = Uuid::new_v4();
 * let record = TurnRecord {
 *     round: 1,
 *     turn,
 *     combatants: Vec::new(),
 *     effects: Vec::new(),
 * };
 * assert_eq!(record.turn, turn);
 * assert!(record.combatants.is_empty());
 * ```
 */
export type TurnRecord = { 
/**
 * Round the turn belongs to.
 */
round: number, 
/**
 * The combatant whose turn it is.
 */
turn: string, 
/**
 * The narrowed per-combatant capture (restore re-creates a deleted one).
 */
combatants: Array<CapturedCombatant>, 
/**
 * Anchored effects.
 */
effects: Array<EffectSnapshot>, };
