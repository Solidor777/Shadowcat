<script lang="ts">
  // The GM panel wrapper the contribution registers; the shared AssetBrowser
  // body also serves the pick-mode overlay.
  import AssetBrowser from "./AssetBrowser.svelte";
</script>

<AssetBrowser mode="manage" />
