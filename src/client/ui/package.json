{
  "name": "@shadowcat/ui",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "dependencies": {
    "@shadowcat/core": "workspace:*"
  },
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "typecheck": "svelte-check --tsconfig ./tsconfig.json",
    "test": "vitest run"
  },
  "devDependencies": {
    "@testing-library/svelte": "^5.3.1",
    "jsdom": "^29.1.1"
  }
}
